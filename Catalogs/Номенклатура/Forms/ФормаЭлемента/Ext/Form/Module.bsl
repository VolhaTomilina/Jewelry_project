
&НаКлиенте
Процедура НаличиеВставкиПриИзменении(Элемент)
	Если Объект.НаличиеВставки Тогда
		Элементы.Вставка.Доступность = Истина;
		Элементы.Вставка.Видимость = Истина;
	Иначе
		Элементы.Вставка.Доступность = Ложь;
		Элементы.Вставка.Видимость = Ложь;
	КонецЕсли;
КонецПроцедуры
